这个脚本就是要安装rust编译所依赖的C++编译器和工具链。需要改造一下脚本，才能在Windows上正常工作。
安装选择：
1、MSVC + Rust工具链
2、GNU gcc + Rust工具链


══════════════════════════════════════════
  C/C++ 编译工具已就绪
══════════════════════════════════════════

[2/4] 检查 Rust 工具链
  ✓ Rust 已安装：rustc 1.95.0 (59807616e 2026-04-14)
    host：x86_64-pc-windows-gnu

检查 Rust 工具链完，现在显示以下内容：
```
═══ 配置 Rust 工具链（x86_64-pc-windows-gnu）═══
  ✓ Rust 工具链 stable-x86_64-pc-windows-gnu 已安装
```

需要改造和上面"检查 C/C++ 编译器"一样
```
══════════════════════════════════════════
  C/C++ 编译工具已就绪
══════════════════════════════════════════
```

这几个ps1脚本，是通过sh转换过来，只需要保持功能一致，ps1的脚本还是需要优化到极致，不能有任何冗余，不能有任何不合理的操作和代码编写。

修改后的脚本需要保持能在Windows上正常工作，不能有任何错误，达到最优性能后才能退出，

每修改一处，都需要提交到git仓库，并且测试通过，才能继续修改。

# 00100

需要优化卸载的确认逻辑，在选择卸载内容时，比如选择1，3，3时，只需要确认1次，后面小的内容，不需要确认了。

```
  ⚠  本脚本会卸载 Android 开发工具，可能影响其它项目。请确认你了解每一步。

请选择要卸载的内容：
  1) Rust Android 编译目标（保留 Android SDK）
  2) Android SDK 目录 + 环境变量（保留 Rust Android targets）
  3) 全部卸载（Rust targets + Android SDK + 环境变量）
  0) 退出（不操作）

  请选择 [0-3]: 3
  ⚠  即将依次卸载：Rust Android targets → Android SDK 目录 → 环境变量
  ? 确认执行全部卸载（请慎重） —— 是否卸载？ [y/N]: y
  ⚠  未检测到任何 Android Rust 编译目标，跳过
  ⚠  删除 SDK 目录将移除以下组件：platform-tools / cmdline-tools / ndk / platforms / build-tools
  ? 删除整个 Android SDK 目录：C:\DevDisk\DevTools\AndroidSDK —— 是否卸载？ [y/N]: y
  ⚠  正在结束 adb / Android Studio / Gradle 相关进程...
  ✓  进程清理完成
  ✓  Android SDK 目录已删除：C:\DevDisk\DevTools\AndroidSDK
  ? 清理 ANDROID_HOME / ANDROID_NDK_HOME 用户环境变量及 PATH 中的 platform-tools 段 —— 是否卸载？ [y/N]: y
  ✓  ANDROID_HOME 已从用户环境变量移除
  ✓  ANDROID_NDK_HOME 已从用户环境变量移除
  ✓  用户 PATH 中的 platform-tools 段已清理
  ✓  环境变量清理完成（新开终端窗口后生效）
```

# 00200
其他几个ps1脚本，也需要类似处理。

#00300
当前脚本：install_android_sdk_bywin.ps1在执行下载url时，会显示以下内容：
Writing web request
Writing request stream... (Number of bytes written: 72214288)

如何做到显示下载进度，以及下载速度 。


#00400

当前脚本：install_android_sdk_bywin.ps1在执行下载url或解压时，会显示以下内容：
Accept? (y/N): [===                                    ] 10% Installing Android SDK Build-Tools
[===                                    ] 10% Downloading build-tools_r34-window
[====                                   ] 10% Downloading build-tools_r34-window
[====                                   ] 11% Downloading build-tools_r34-window
[====                                   ] 12% Downloading build-tools_r34-window
[=====                                  ] 13% Downloading build-tools_r34-window
[=====                                  ] 14% Downloading build-tools_r34-window
[=====                                  ] 15% Downloading build-tools_r34-window
[======                                 ] 15% Downloading build-tools_r34-window
[======                                 ] 16% Downloading build-tools_r34-window
[======                                 ] 16% Unzipping...
[======                                 ] 16% Unzipping... android-14/
[======                                 ] 16% Unzipping... android-14/lib/
[======                                 ] 16% Unzipping... android-14/lib64/
[======                                 ] 16% Unzipping... android-14/lld-bin/
[======                                 ] 16% Unzipping... android-14/renderscri
[======                                 ] 16% Unzipping... android-14/aapt.exe
[======                                 ] 16% Unzipping... android-14/aapt2.exe
[======                                 ] 16% Unzipping... android-14/aarch64-li
[======                                 ] 16% Unzipping... android-14/aidl.exe
[======                                 ] 16% Unzipping... android-14/apksigner.

目前显示的内容有宽度控制，如何做到显示完整的内容。前面的进度条可以不显示，只需要显示百分比和当前解压的文件名。



# 00500
这个脚本：build_bywin.ps1 有一些安装检查，比如C/C++ 编译器、Rust、Android SDK等，在另两个脚本已经体现，这个脚本只需要检查Rust Android 编译目标即可，如果没有安装不需要安装，提示执行某脚本安装就行。

Write-Host "[6/8] Rust Android 编译目标" -ForegroundColor Cyan


# 00600
这个脚本：build_bywin.ps1 生成keystore.properties文件，storeFile默认值为./config/release.keystore

$DefaultKeystoreLines = @(
  'keyAlias=tauri2demo_key',
  'password=abc009988',
  'storeFile="C:\SyncData\release.keystore"'
)
需要修改storeFile为./config/release.keystore

# 00700
这个脚本：build_bywin.ps1 需要增加一个参数，只是做检查，不执行构建。参数名：-CheckOnly，默认值为false。

如果-CheckOnly的值为true，则执行检查，在控制台显示以下内容后退出：
```
══════════════════════════════════════════
  所有检查通过！
══════════════════════════════════════════
```
如果-CheckOnly的值为false，则继续执行构建。

# 00800
检测到Android SDK，需要显示具体版本号
修改以下代码
```
Write-Host "[4/8] Android SDK" -ForegroundColor Cyan
$androidHome = Resolve-AndroidHome
if ($null -ne $androidHome) {
  $env:ANDROID_HOME = $androidHome
  Write-Ok "ANDROID_HOME=$androidHome"
} else {
  Write-Fail "ANDROID_HOME 未设置且未检测到 Android SDK"
  Write-Fail "请运行 .\script\install_android_sdk_bywin.ps1 安装"
}
```

# 00900

执行命令：./build_bywin.ps1 build -y 时，报以下错误：
```
══════════════════════════════════════════
  构建准备
══════════════════════════════════════════

[准备 1/4] npm 依赖
  ✓  node_modules 已存在
[准备 2/4] Tauri Android 项目
  ⚠  settings.gradle.kts 缺失
  ⚠  已备份 keystore.properties
  ⚠  正在删除不完整的 gen\android 目录 ...
  ⚠  正在运行 pnpm tauri android init ...
'tauri' is not recognized as an internal or external command,
operable program or batch file.
undefined
 ERR_PNPM_RECURSIVE_EXEC_FIRST_FAIL  Command "tauri" not found

Did you mean "pnpm start"?
Copy-Item : Could not find a part of the path 'C:\MyWork\tmpcode\tauri2demo\backend\src-tauri\gen\android\keystore.properties'.
At C:\MyWork\tmpcode\tauri2demo\script\build_bywin.ps1:59 char:5
+     Copy-Item -LiteralPath $keystoreBackup -Destination $keystoreProp ...
+     ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: (:) [Copy-Item], DirectoryNotFoundException
    + FullyQualifiedErrorId : System.IO.DirectoryNotFoundException,Microsoft.PowerShell.Commands.CopyItemCommand

```
需要修改报错，修改完自动测试，测试成功后，进入一处修改，反复直到全部优化完成，测试通过才能退出。


# 01000
当前有多个软件源，下载时现在会报错，请修复它。

```
$ winget install --id Microsoft.WindowsTerminal
Failed when searching source: msstore
An unexpected error occurred while executing the command:
0x8a15005e : The server certificate did not match any of the expected values.

The following packages were found among the working sources.
Please specify one of them using the --source option to proceed.
Name             Id                        Source
-------------------------------------------------
Windows Terminal Microsoft.WindowsTerminal winget

C:\Users\Admin>winget source list
Name    Argument
-----------------------------------------------------
winget  https://mirrors.ustc.edu.cn/winget-source
msstore https://storeedgefd.dsx.mp.microsoft.com/v9.0
```

Found Windows Terminal [Microsoft.WindowsTerminal] Version 1.24.10921.0
This application is licensed to you by its owner.
Microsoft is not responsible for, nor does it grant any licenses to, third-party packages.
Downloading https://github.com/microsoft/terminal/releases/download/v1.24.10921.0/Microsoft.WindowsTerminal_1.24.10921.0_8wekyb3d8bbwe.msixbundle


# 01100
找到winget命令的PowerShell Module，可以直接通过接口调用，不需要通过命令行下载，这样进度条就好控制。
```
PowerShell Module
The Microsoft.WinGet.Client PowerShell module provides native cmdlets for interacting with the Windows Package Manager. You can install it from the PowerShell Gallery:

Install-Module -Name Microsoft.WinGet.Client
Example usage:

# Search for a package
Find-WinGetPackage -Query "Microsoft.WindowsTerminal"

# Install a package
Install-WinGetPackage -Id Microsoft.WindowsTerminal

# List installed packages
Get-WinGetPackage

# Update a package
Update-WinGetPackage -Id Microsoft.WindowsTerminal

# Repair the WinGet package manager installation
Repair-WinGetPackageManager
For more information, see the Microsoft.WinGet.Client page on the PowerShell Gallery.

```

C:\MyWork\TmpCode\tauri2demo\script
./install_base_tools_bywin.ps1 -y -WingetMode module -RemoveTools winget
./install_base_tools_bywin.ps1 -y -WingetMode module -AddTools terminal


为何我在PowerShell中调用Get-WinGetPackage会报错
```
Get-WinGetPackage
Get-WinGetPackage : Error occurred when loading Windows Package Manager Group Policy Settings
At line:1 char:1
+ Get-WinGetPackage
+ ~~~~~~~~~~~~~~~~~
    + CategoryInfo          : NotSpecified: (:) [Get-WinGetPackage], GroupPolicyException
    + FullyQualifiedErrorId : Microsoft.WinGet.SharedLib.Exceptions.GroupPolicyException,Microsoft.WinGet.Client.Commands.GetPackageCmdlet
```


https://hub.gitcode.com/repos/microsoft/terminal/releases/latest

# 01200

需要在 install_base_tools_bywin.ps1 脚里里，显示以下信息
```
  ✓  winget 已安装
    路径：C:\Users\tony\AppData\Local\Microsoft\WindowsApps\winget.exe
    版本：v1.28.240
```
增加显示文件大小的功能。

# 01300

为何当前winget已经安装了，但是显示的大小是0.00 MB？
```
  ✓  winget 已安装
    路径：C:\Users\tony\AppData\Local\Microsoft\WindowsApps\winget.exe
    大小：0.00 MB
    版本：v1.28.240
```

C:\Users\Admin\.qshell\tea4get.exe --log_short -l=5 wr install-nvm.ps1
C:\Users\Admin\.qshell\tea4get.exe --log_short -l=5 i sysinfo

# 01400
需要在 install_base_tools_bywin.ps1 脚里里，Save-WebFile函数增加，如果下载速度过慢（例如：小于300KB/s），则尝试从下一个地址库下载。
如果所有地址库都失败了，再报错。
