# MyTodos 团队待办 App — 测试用例文档

## 1. 文档概述

### 1.1 文档目的

本文档基于《1-SRS需求.md》和《2-PRD设计.md》，为 MyTodos 定义测试用例，覆盖所有功能需求（FR）的验证场景，包括正常路径、异常路径和权限边界。

### 1.2 测试范围

- 功能测试：覆盖 FR-001 ~ FR-006, FR-101 ~ FR-106
- 权限测试：覆盖角色权限矩阵
- 接口测试：Gitee API 交互与错误处理
- 并发测试：多端冲突场景
- UI 测试：页面跳转与组件交互

### 1.3 测试环境

- 测试设备：Android 中端机 / iOS 中端机 / Windows 桌面端
- 网络环境：正常网络 / 弱网(模拟) / 断网
- 测试数据：预置标准工作区（含 4 成员、11 标签、若干任务）

---

## 2. 账户与工作区测试

### TC-001 Gitee 凭证配置

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-001 |
| **前置条件** | `.env` 中配置了有效的 GITEE_PAT |
| **优先级** | P0 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 启动应用 | PAT 随编译注入，应用正常启动 |
| 2 | 调用任意 Gitee API | 请求头携带 `Authorization: token {PAT}` |
| 3 | 在 `.env` 中填写无效 PAT，重新编译启动 | 首次 API 调用返回 401，显示"凭证无效"提示 |

---

### TC-002 首次进入 — 创建工作区（管理员）

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-002, FR-003 |
| **前置条件** | 首次打开应用（无本地存储） |
| **优先级** | P0 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 打开应用且全局配置中无任何工作区 | 显示引导页，仅显示"创建工作区"入口（已取消"加入工作区"） |
| 2 | 点击"创建工作区" | 弹出表单：工作区名称、描述、管理员显示名、管理员 6 位密码 |
| 3 | 填写名称"测试工作区"、管理员名"老王"、密码 "123456"，提交 | 创建成功，global.members 含一条 admin 记录（含 password），自动登录并跳转成员管理页 `/workspaces/:id/members` |
| 4 | 关闭应用后重新打开 | 跳过引导页，直接进入成员管理页（admin 身份保留） |

---

### TC-003 全局配置工作区列表读取

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-001, FR-002 |
| **前置条件** | 全局配置 gist 中已有 ≥1 个工作区 |
| **优先级** | P0 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 启动应用 | 自动从 GLOBAL_GIST_ID 拉取全局配置 |
| 2 | 进入工作区列表（UI-002） | 显示从全局配置读到的所有工作区，无须本地"加入"操作 |
| 3 | 点击某工作区 | 跳转该工作区登录页 `/workspaces/:id/login` |
| 4 | 登录页显示 | 仅列出 role 为 parent / student 的成员卡片，admin 不出现；右上角有管理员入口图标 |
| 5 | 全局配置 gist 不可达（网络异常） | 显示错误提示"无法加载全局配置"，可重试 |

---

### TC-004 工作区登录页 — 家长/学生登录

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-002 |
| **前置条件** | 已加入工作区，已位于 `/workspaces/:id/login` |
| **优先级** | P0 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击家长成员卡片 | 弹出 6 位密码输入框 |
| 2 | 输入错误密码 "111111" | 提示"密码错误"，可重新输入 |
| 3 | 输入正确密码 "234567" | 验证通过，保存 currentMemberId/密码到本地，进入 `/workspaces/:id/tasks`，可创建/编辑/指派任务 |
| 4 | 退出登录 → 选学生成员 → 输入正确密码 "345678" | 进入 `/workspaces/:id/tasks`，仅可处理分配给自己的任务 |

---

### TC-004a 工作区登录页 — 管理员入口

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-002 |
| **前置条件** | 位于 `/workspaces/:id/login`，该工作区 global.members 中有 role=admin 的成员 |
| **优先级** | P0 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击右上角管理员入口图标 | 弹出管理员密码输入框，显示该 admin 成员的 displayName |
| 2 | 输入错误密码 | 提示"密码错误"，可重新输入 |
| 3 | 输入正确密码 | 以 admin 身份登录，跳转 `/workspaces/:id/admin`（管理员主页，UI-200） |
| 4 | 工作区无 admin 成员（异常场景） | 弹窗显示"该工作区暂无管理员"提示，无法登录 |

---

### TC-004b 管理员主页（UI-200）

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-005 / FR-006 / FR-003（含工作区编辑） |
| **前置条件** | 以管理员身份登录某工作区 |
| **优先级** | P0 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 进入 `/workspaces/:id/admin` | TopBar 标题「后台管理」；右上角显示退出登录图标按钮（与 UI-101 同款 SVG，白色） |
| 2 | 点击"成员管理" | 跳转 `/workspaces/:id/members` |
| 3 | 在成员管理页点返回 | 回到 `/workspaces/:id/admin` |
| 4 | 点击"标签管理" | 跳转 `/workspaces/:id/tags`；返回回到管理员主页 |
| 5 | 点击"工作区设置" | 跳转 `/workspaces/:id/settings`，可编辑名称/描述、查看 gistId |
| 6 | 点击右上角退出图标 | 跳到 `/workspaces/:id/login` |

---

### TC-005 创建工作区

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-003 |
| **前置条件** | 以管理员角色登录 |
| **优先级** | P0 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击"创建工作区" | 弹出表单：工作区名称、描述、管理员显示名、管理员 6 位密码 |
| 2 | 名称为空，点确认 | 提示"工作区名称不能为空" |
| 3 | 名称超 100 字 | 提示"名称过长" |
| 4 | 管理员密码非 6 位数字 | 提示"密码须为 6 位数字" |
| 5 | 填写名称"测试工作区"、管理员名"老王"、密码 "123456"，确认 | 创建成功，生成 gistId，global.members 包含 admin 记录且带 password，跳转工作区详情 |
| 6 | 检查 Gitee 代码片段 | 存在对应 gist，含 meta.json 和 todos.json，且 meta.json 不存在 `passwords` 顶层字段 |

---

### TC-006 切换工作区

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-004 |
| **前置条件** | 全局配置 gist 中至少有两个工作区，已登录其一 |
| **优先级** | P1 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 进入工作区列表 | 显示所有来自全局配置的工作区，无"加入"按钮 |
| 2 | 点击非当前已登录工作区 | 跳转目标工作区的登录页 `/workspaces/:id/login` |
| 3 | 在目标工作区完成登录 | 任务列表/管理员主页加载新工作区数据 |
| 4 | 切回原工作区 | 同样需重新登录 |

---

### TC-007 成员管理

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-005 |
| **前置条件** | 以管理员角色登录某工作区 |
| **优先级** | P0 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 进入"管理成员"页面 | 显示当前成员列表 |
| 2 | 点击"添加成员" | 弹出表单：显示名、角色、6 位数字密码（密码字段缺省值为 `123456`） |
| 3 | 显示名为空提交 | 提示"名称不能为空" |
| 4 | 把密码改成非 6 位数字提交 | 提示"密码须为 6 位数字" |
| 5 | 填写"小明"、角色"学生"，密码保持缺省值 `123456`，提交 | 列表新增"小明"（role: student），global.members 中该条记录 password 为 `123456` |
| 6 | 点击编辑"小明" | 弹出表单仅显示"显示名"与"重置密码"两项；**不显示角色字段**（角色不可修改） |
| 7 | 编辑"小明"，重置密码为 "654321" | 更新成功，global.members 中该成员 password 字段被覆盖 |
| 8 | 编辑"小明"显示名为"大明" | 更新成功 |
| 9 | 移除"大明" | 列表移除，总数回到之前 |
| 10 | 尝试添加第 6 人 | 提示"成员已达上限（5 人）" |

---

### TC-008 标签管理

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-006 |
| **前置条件** | 以管理员角色登录工作区 |
| **优先级** | P1 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 进入"管理标签"页面 | 显示当前标签列表（11 个初始标签） |
| 2 | 点击"新建标签" | 弹出表单：名称、颜色 |
| 3 | 名称为空提交 | 提示"标签名称不能为空" |
| 4 | 填写"编程"、选择颜色，提交 | 列表新增"编程"标签 |
| 5 | 编辑"编程"颜色 | 颜色更新成功 |
| 6 | 删除"编程" | 列表移除该标签 |
| 7 | 连续添加标签至 21 个 | 提示"标签已达上限（20 个）" |

---

## 3. 任务管理测试

### TC-101 创建任务（家长）

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-101 |
| **前置条件** | 以家长角色登录 |
| **优先级** | P0 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击"新增任务" | 弹出表单；**不显示「状态」字段**（创建时默认 `todo`）；截止日期默认值为当天本地时间 21:00；**优先级以按钮组形式呈现（低/中/高），默认选中"中"** |
| 2 | 标题为空，点提交 | 提示"标题不能为空" |
| 3 | 标题超过 80 字 | 输入限制，无法继续输入 |
| 4 | 描述超过 256 字 | 输入限制 |
| 5 | 不选截止日期 | 提示"截止日期不能为空" |
| 6 | 查看「指派人」下拉 | 仅列出 role=student 的成员；不出现 admin/parent |
| 7 | 不选指派人 | 提示"指派人不能为空" |
| 8 | 查看「标签」下拉 | 单选下拉，第一项为"（无）"，其余为全局标签 |
| 9 | 正确填写所有必填项，提交 | 创建成功，列表刷新显示新任务，初始 status=todo |
| 10 | 选择某标签后提交 | 任务 tagIds 数组只含 1 个 tagId；列表项显示该标签色块 + 名称 |

---

### TC-102 创建任务（学生越权）

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-101 (权限验证) |
| **前置条件** | 以学生角色登录 |
| **优先级** | P0 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 查看任务列表 | 底部无"新增任务"按钮 |
| 2 | 直接访问创建任务路由 | 路由守卫拦截，跳回任务列表 |

---

### TC-103 编辑任务（家长）

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-102 |
| **前置条件** | 以家长角色登录，存在已创建任务 |
| **优先级** | P0 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击任务进入详情 | 显示完整字段 |
| 2 | 点击"编辑" | 字段变为可编辑状态 |
| 3 | 修改标题、优先级、指派人 | 保存成功，`updatedAt`/`updatedBy` 更新 |
| 4 | 修改描述为空 | 保存成功（描述可选） |

---

### TC-104 编辑任务（学生越权）

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-102 (权限验证) |
| **前置条件** | 以 Jane 学生角色登录，存在分配给 Jane 的任务 |
| **优先级** | P0 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 点击任务进入详情 | 显示查看模式，无"编辑"按钮 |

---

### TC-105 处理任务 — 开始进行中

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-103, FR-107 |
| **前置条件** | 以 Jane 学生登录，存在分配给 Jane 的待办任务 |
| **优先级** | P0 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 在任务列表中滑动或点击"开始" | 状态变为"进行中" |
| 2 | 查看任务详情 | `startedAt` 已记录当前时间，`status: "doing"` |
| 3 | 详情页"开始时间"行 | 显示开始时间 + 「（已过 N 分钟）」耗时提示，每分钟自动刷新 |
| 4 | 等待至下一分钟后再次查看 | 「已过」分钟数自动 +1（无需刷新页面） |

---

### TC-106 处理任务 — 标记完成

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-103, FR-107 |
| **前置条件** | 以 Jane 学生登录，存在进行中的任务 |
| **优先级** | P0 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 在任务列表点击"完成"复选框 | 状态变为"已完成" |
| 2 | 查看任务详情 | `completedAt` 已记录，`completedBy` = Jane 的 memberId |
| 3 | 详情页"开始时间"行 | 显示「（耗时 …）」，差值 = `completedAt - startedAt`，不再随时间变化 |
| 4 | 详情页"完成时间"行 | 显示完成时间（本地化日期时间） |

---

### TC-107 处理任务 — 恢复待办

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-103 |
| **前置条件** | 存在已完成的任务 |
| **优先级** | P1 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 家长角色，点击已完成任务的"恢复" | 状态变为"待办"，`startedAt`/`completedAt`/`completedBy` 清除 |
| 2 | 学生角色，点击自己已完成任务的"恢复" | 状态变为"待办"，相关字段清除 |

---

### TC-108 处理任务（管理员越权）

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-103 (权限验证) |
| **前置条件** | 以管理员角色登录 |
| **优先级** | P1 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 进入任务详情 | 只读模式，无"开始"/"完成"/"恢复"按钮 |

---

### TC-109 删除任务

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-104 |
| **前置条件** | 以家长角色登录 |
| **优先级** | P0 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 滑动任务项，点击"删除" | 弹出确认对话框 |
| 2 | 点击"取消" | 对话框关闭，任务不变 |
| 3 | 点击"确认删除" | 任务从列表消失 |
| 4 | 检查远端数据 | 任务仍在 todos.json，`deletedAt`/`deletedBy` 已记录 |

---

### TC-110 删除任务（学生/管理员越权）

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-104 (权限验证) |
| **前置条件** | 分别以学生、管理员角色登录 |
| **优先级** | P1 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 学生：滑动任务项 | 无"删除"选项 |
| 2 | 管理员：进入任务详情 | 无"删除"按钮 |

---

### TC-111 任务列表 — 家长视图筛选

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-105 |
| **前置条件** | 以家长角色登录，存在各种状态的任务 |
| **优先级** | P0 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 查看默认列表 | 顶部 TopBar 标题显示「分配任务」；筛选条件默认 状态=待办、截止=今天，仅显示符合条件的任务，按默认排序 |
| 2 | 切换状态筛选为"已完成" | 只显示已完成任务 |
| 3 | 按指派人筛选 "Jane" | 只显示分配给 Jane 的任务 |
| 4 | 把截止日期切换为"全部时间" | 显示所有未删除任务 |
| 5 | 按标签"数学"筛选 | 只显示标签含"数学"的任务 |
| 6 | 组合筛选：待办 + Jane + 数学 | 同时满足三个条件的任务 |
| 7 | 列表项显示 | 每个任务行包含标题、截止时间、指派人、优先级标识、**标签色块 + 名称**（无标签时不显示标签区） |

---

### TC-112 任务列表 — 学生视图

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-105 |
| **前置条件** | 以 Jane 学生角色登录 |
| **优先级** | P0 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 进入任务列表 | TopBar 标题显示「处理任务」；顶部出现 3 个按钮（待办 / 进行中 / 完成），默认选中"待办"；每个按钮右侧显示该状态下任务数量的红色数字角标（数量为 0 时不显示） |
| 2 | 列表内容 | 仅显示 `assigneeId === 自己 memberId` 的任务，且符合"待办"状态；不可见其他成员的任务 |
| 3 | 切换"进行中" | 仅显示自己 status=doing 的任务 |
| 4 | 切换"完成" | 仅显示自己 status=done 的任务 |
| 5 | 检查列表项 | 每行包含标题、截止时间、指派人、优先级、**标签色块 + 名称** |
| 6 | 检查排序 | 逾期优先 → 截止升序 → 创建降序 |
| 7 | 通过 URL 直接访问任意任务详情 | 若该任务非自己 assignee，则视为越权或不可见 |

---

### TC-113 任务搜索

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-106 |
| **前置条件** | 以家长角色登录 |
| **优先级** | P1 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 进入任务列表 | **默认搜索输入框不显示**，TopBar 右上角有搜索图标按钮 |
| 2 | 点击搜索图标 | 搜索输入框展开显示，可输入关键词 |
| 3 | 搜索"数学" | 匹配标题或描述包含"数学"的任务 |
| 4 | 搜索不存在的关键词 "xyz" | 显示空状态提示 |
| 5 | 清空搜索词或再次点击搜索图标 | 输入框可隐藏，恢复原列表 |
| 6 | 学生角色搜索 | 仅在自己 assigneeId 范围内搜索，他人任务不出现在结果中 |
| 7 | 搜索 + 筛选组合 | 同时满足搜索和筛选条件 |

---

### TC-114 任务详情字段展示（含标签 / 开始时间 / 完成时间）

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-107, UI-102 |
| **前置条件** | 任意角色登录，存在任意任务 |
| **优先级** | P1 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 进入任务详情页 | 完整字段顺序：标题、描述、状态、优先级、截止、指派、开始时间、完成时间、标签 |
| 2 | 任务无 startedAt / completedAt / tagIds | "开始时间"、"完成时间"、"标签"行均显示 `—` |
| 3 | 任务有 startedAt 但未完成 | "开始时间"显示日期 + 「（已过 N 分钟 / N 小时 M 分钟 / N 天 M 小时）」，每分钟自动刷新 |
| 4 | 任务已完成 | "开始时间"显示日期 + 「（耗时 …）」（差值固定为 completedAt - startedAt）；"完成时间"显示完成时间 |
| 5 | 任务关联标签 | "标签"行显示色块 + 名称（单个） |

---

## 4. 接口与异常测试

### TC-201 网络断开

| 项目 | 内容 |
|------|------|
| **对应 FR** | SRS 4.1, NFR-101 |
| **前置条件** | 已登录任意角色 |
| **优先级** | P0 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 断开设备网络 | 顶部显示"网络已断开" |
| 2 | 尝试新增/编辑任务 | 操作按钮置灰或弹出"网络不可用"提示 |
| 3 | 恢复网络 | 状态恢复，自动刷新数据 |

---

### TC-202 API 401/403 错误

| 项目 | 内容 |
|------|------|
| **对应 FR** | SRS 4.1 |
| **前置条件** | PAT 无效或过期 |
| **优先级** | P1 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 编译时使用无效 PAT | API 调用返回 401 |
| 2 | 应用显示错误提示 | "凭证无效或权限不足"，引导重新配置 |

---

### TC-203 API 404 错误

| 项目 | 内容 |
|------|------|
| **对应 FR** | SRS 4.1 |
| **前置条件** | gistId 不存在 |
| **优先级** | P1 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 输入不存在的 gistId 加入 | 提示"工作区不存在" |

---

### TC-204 并发冲突

| 项目 | 内容 |
|------|------|
| **对应 FR** | SRS 2.4, 6.3 |
| **前置条件** | 两台设备同时登录同一工作区 |
| **优先级** | P1 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 设备A编辑任务标题为"AAA" | 编辑中... |
| 2 | 设备B编辑同一任务标题为"BBB"先提交 | 设备B提交成功，revision 变更 |
| 3 | 设备A提交 | 检测到冲突，提示"数据已被修改" |
| 4 | 用户选择"保留我的" | 以"AAA"覆盖 |
| 5 | 用户选择"保留远端的" | 丢弃"AAA"，显示"BBB" |

---

## 5. UI 跳转测试

### TC-301 引导页跳转

| 项目 | 内容 |
|------|------|
| **对应 FR** | UI-001, UI-002a |
| **前置条件** | 首次使用 |
| **优先级** | P1 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 创建工作区后 | 自动以管理员登录，跳转 `/workspaces/:id/members` |
| 2 | 加入工作区后 | 跳转该工作区登录页 `/workspaces/:id/login`，未触发 login |
| 3 | 在登录页选家长 + 输正确密码 | 跳 `/workspaces/:id/tasks` |
| 4 | 在登录页选学生 + 输正确密码 | 跳 `/workspaces/:id/tasks` |
| 5 | 在登录页通过右上角管理员入口 + 正确密码 | 跳 `/workspaces/:id/members` |

---

### TC-302 路由守卫 — 未登录

| 项目 | 内容 |
|------|------|
| **对应 FR** | UI-001, UI-002a |
| **前置条件** | 清除所有本地 session（仍保留工作区列表） |
| **优先级** | P1 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 直接访问 `/workspaces/:id/tasks` | 守卫拦截，跳 `/workspaces` 列表 |
| 2 | 在 `/workspaces` 点击工作区 | 跳 `/workspaces/:id/login`，可在未登录状态访问 |
| 3 | 直接访问 `/workspaces/:id/login` | 允许访问，正常显示登录页 |
| 4 | 本地工作区列表为空时直接访问 `/workspaces` | 跳 `/guide` |

---

### TC-304 退出登录后跳转

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-002, UI-002a |
| **前置条件** | 已以家长/学生/管理员身份登录某工作区 |
| **优先级** | P1 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 在任务列表（家长/学生）点击退出 | 跳 `/workspaces/:id/login`，保留当前工作区上下文 |
| 2 | 在成员管理（管理员）点击退出 | 跳 `/workspaces/:id/login` |
| 3 | 退出后再次直接访问 `/workspaces/:id/tasks` | 守卫拦截，跳 `/workspaces` 或 `/workspaces/:id/login` |

---

### TC-303 路由守卫 — 越权访问

| 项目 | 内容 |
|------|------|
| **对应 FR** | UI-103 |
| **前置条件** | 以家长角色登录 |
| **优先级** | P1 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 直接访问 `/workspaces/:id/tags` | 跳转到任务列表 |
| 2 | 直接访问 `/workspaces/:id/members` | 跳转到任务列表 |

---

## 6. 数据一致性测试

### TC-401 任务创建后远端存储

| 项目 | 内容 |
|------|------|
| **对应 FR** | SRS 6.1 |
| **前置条件** | 家长创建任务成功 |
| **优先级** | P0 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 创建任务 "测试数据一致性" | 创建成功 |
| 2 | 通过 Gitee Web 查看代码片段 | todos.json 中包含该任务完整数据 |
| 3 | 另一设备刷新 | 可看到新任务 |

---

### TC-402 逻辑删除验证

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-104 |
| **前置条件** | 家长删除任务 |
| **优先级** | P1 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 删除任务后检查远端 todos.json | 任务数据仍存在 |
| 2 | 检查 `deletedAt` 和 `deletedBy` | 已记录删除时间和操作人 |
| 3 | 列表中不显示 | 已删除任务不出现在任何视图中 |

---

## 7. 边界条件测试

### TC-501 空工作区

| 项目 | 内容 |
|------|------|
| **优先级** | P2 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 新建工作区，0 条任务 | 任务列表显示空状态提示 |
| 2 | 所有筛选器 | 均显示 0 结果 |
| 3 | 搜索 | 空状态提示 |

---

### TC-502 成员密码修改场景

| 项目 | 内容 |
|------|------|
| **对应 FR** | FR-002, FR-005 |
| **优先级** | P2 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 管理员在成员管理中重置某家长成员的密码为新值 | 该家长成员 global.members[i].password 更新成功 |
| 2 | 已登录该家长成员的设备下次启动 | 自动登录（本地已保存旧密码与 memberId，离线流程不强制远端比对） |
| 3 | 新设备用旧密码以该家长身份加入工作区 | 验证失败，提示"密码错误" |
| 4 | 新设备用新密码以该家长身份加入工作区 | 验证通过，登录成功 |

---

### TC-503 应用图标

| 项目 | 内容 |
|------|------|
| **对应 FR** | NFR-301 |
| **优先级** | P2 |

| 步骤 | 操作 | 预期结果 |
|------|------|----------|
| 1 | 安装 Windows / macOS / Linux 桌面包 | 桌面/启动菜单中应用图标为剪贴板 + 勾选样式（来源：clipboard-todo.svg） |
| 2 | 安装 Android APK | Launcher 中应用图标一致 |
| 3 | 检查 `backend/src-tauri/icons/` 各分辨率 ico/png/icns | 均由 `pnpm tauri icon` 自统一 SVG 源生成，无遗漏分辨率 |

---

## 8. 测试用例统计

| 类别 | 用例数 | P0 | P1 | P2 |
|------|--------|----|----|-----|
| 账户与工作区 | 8 | 5 | 3 | 0 |
| 任务管理 | 14 | 8 | 6 | 0 |
| 接口与异常 | 4 | 1 | 3 | 0 |
| UI 跳转 | 3 | 0 | 3 | 0 |
| 数据一致性 | 2 | 1 | 1 | 0 |
| 边界条件 | 3 | 0 | 0 | 3 |
| **合计** | **34** | **15** | **16** | **3** |

**优先级说明：**
- **P0**：核心功能，必须全部通过
- **P1**：重要功能，需全部通过
- **P2**：边缘场景，建议通过
